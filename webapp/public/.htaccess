Options -MultiViews
RewriteEngine On
RewriteBase /app/
# If a requested resource isn’t found, serve index.html
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.html [QSA,L]
